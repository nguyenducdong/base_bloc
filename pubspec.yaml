name: base_bloc
description: A base BloC (Business Logic Component) class for Flutter applications that provides common state management operations, resource cleanup, and event/state stream handling.
version: 1.0.0
repository: https://github.com/nguyenducdong/base_bloc

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  rxdart: ^0.27.0

dev_dependencies:
  test: ^1.16.0
